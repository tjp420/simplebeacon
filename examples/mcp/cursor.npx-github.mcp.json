{
  "mcpServers": {
    "simplebeacon": {
      "command": "npx",
      "args": ["--yes", "simplebeacon-mcp", "--offline"],
      "env": {
        "SIMPLEBEACON_PROJECT_ROOT": "${workspaceFolder}",
        "SIMPLEBEACON_OFFLINE": "1"
      }
    }
  }
}
